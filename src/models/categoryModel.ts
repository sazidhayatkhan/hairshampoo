export interface CategoryModel {
    id: string;
    createdAt: string;
    updatedAt: string;
    isDeleted: boolean;
    title: string;
    description: string;
    icon: string;
    urlSlug: string;
}
